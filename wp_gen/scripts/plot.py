
    # wp_gen = Wp_gen(7.5, 1.35, 244, 244)

    # x, y1, y2, y3, x_target, y_target = wp_gen.convert_origin(m1, m2, b1, b2)

    # image = cv2.imread('../figures/test2.jpeg')

    # plt.figure()
    # height, width, _ = image.shape
    # extent = [-width/2, width/2, 0, height]
    # plt.imshow(cv2.cvtColor(image, cv2.COLOR_BGR2RGB), extent=extent)

    # #Plot the lines
    # plt.plot(x, y1, label='$y_1 = m_1\cdot x + b_1$')  # Line 1
    # plt.plot(x, y2, label='$y_2 = m_2 \cdot x + b_2$')  # Line 2
    # plt.plot(x, y3, label='$y_3 = m_3 \cdot x + b_3$')  # Line 2
    # plt.scatter(x_target, y_target, color='red', label='Target')  # Plotting the point (20, 30)

    # plt.xlim(-width/2, width/2)  
    # plt.ylim(0, height) 

    # # Add labels and legend
    # plt.xlabel('x')
    # plt.ylabel('y')
    # plt.title('Plot of Lines')
    # plt.legend()    

    # # Show the plot
    # plt.grid(True)
    # plt.axhline(0, color='black', linewidth=0.5)
    # plt.axvline(0, color='black', linewidth=0.5)
    # plt.show()